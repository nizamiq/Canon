apiVersion: v1
kind: Service
metadata:
  name: canon
  namespace: canon
  labels:
    app: canon
    app.kubernetes.io/name: canon
    app.kubernetes.io/part-of: nizamiq
spec:
  selector:
    app: canon
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
